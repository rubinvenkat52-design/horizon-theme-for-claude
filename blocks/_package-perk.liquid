

{% liquid
  assign block_settings = block.settings
%}

<div
  class="package-perk"
  {{ block.shopify_attributes }}
>
  <span class="package-perk__emoji" aria-hidden="true">{{ block_settings.emoji }}</span>
  <span class="package-perk__text">{{ block_settings.text }}</span>
</div>

{% stylesheet %}
  .package-perk {
    display: flex;
    align-items: center;
    gap: var(--gap-2xs);
    padding: var(--padding-2xs) var(--padding-sm);
    background: rgb(var(--color-foreground-rgb) / var(--opacity-5));
    font-size: var(--font-paragraph-small--size);
  }

  .package-perk:not(:first-child) {
    border-top: var(--style-border-width) solid var(--color-border);
  }

  .package-perk:last-child {
    border-radius: 0 0 var(--style-border-radius-md) var(--style-border-radius-md);
  }

  .package-perk__text {
    font-weight: var(--font-weight-bold);
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Package perk",
  "tag": null,
  "settings": [
    {
      "type": "text",
      "id": "emoji",
      "label": "Emoji",
      "default": "🎁"
    },
    {
      "type": "text",
      "id": "text",
      "label": "Text",
      "default": "+10 FREE Boundary Training Markers ($20 Value)"
    }
  ],
  "presets": [
    {
      "name": "Package perk"
    }
  ]
}
{% endschema %}
